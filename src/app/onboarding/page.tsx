import OnboardingClient from "./OnboardingClient";

export const metadata = {
  title: "온보딩 | Deal Story",
  robots: { index: false },
};

export default function OnboardingPage() {
  return <OnboardingClient />;
}
